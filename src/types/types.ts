export interface ProjectData {
    name: string;
    description: string;
}

export interface TravelData {
    name: string;
    description: string;
    amount: number;
    date: string;
    category: string;
    userId: string;
    projectId: string;
}
